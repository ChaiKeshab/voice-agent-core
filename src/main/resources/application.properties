# Mock or live mode ('mock' to avoid needing OpenAI key now). returns a sample response
voiceagent.mode=live

# OpenAI API key (only needed in live mode)
openai.api.key=sk___

# Server port (optional)
server.port=8080